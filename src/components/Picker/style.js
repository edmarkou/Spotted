import { StyleSheet } from 'react-native';

const style = StyleSheet.create({
    icon_placement: {
        marginRight: 10
    },
    icon: {
        width: 12,
        resizeMode: 'contain'
    }
});

export default style;